version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: love_translator_db
    restart: always
    environment:
      MYSQL_DATABASE: love_db
      MYSQL_ROOT_PASSWORD: qwer1234
    ports:
      - "3307:3306"
    volumes:
      - ./db_data:/var/lib/mysql